# 配信前チェックと設定の保護（v1.0.24）

## 配信前の一括チェック

ダッシュボード上部の「チェックを開始」で、保存済みの設定を確認します。
TikTok ID、設定形式、サーバーファイル、ワールド、RCON設定の一致、ギフトとイベントの参照コマンド、ギフト一覧の更新時刻、サーバー・Mod・プレイヤー・Bridgeの状態、読み上げエンジンの応答を表示します。

「要確認」「要修正」には対応の案内と設定画面へのボタンが表示されます。停止中のサーバーや未接続のLIVEは要確認となります。起動・接続後に再チェックしてください。チェックはギフト送信やゲーム内コマンド実行を行いません。実際のギフト発動は配信接続後に別途確認してください。

## 未保存の変更

ギフト割り当て、コマンド作成、イベント設定①・②、読み上げ、運用設定、ダッシュボードのID・ワールド選択で編集状態を表示します。未保存のまま画面移動・対象画面の再読込・アプリ終了をすると確認が出ます。キャンセルすれば編集を続けられます。保存失敗時も保護を維持します。強制終了やPCの電源断からの下書き復元は含みません。

## 設定バックアップ

運用センター上部の「現在の設定を保存」で手動保存します。ギフト・イベント・読み上げ・運用設定とコマンドTXTを対象とします。保存済みの状態をバックアップするため、編集中の変更は先に各画面で保存してください。

- 設定・コマンドを書き換える前に自動保存します。同じ内容は重複させず、自動保存は30世代を保持します。
- 手動保存と復元直前の保存は自動整理で削除しません。
- 日時・保存理由・ギフト件数で選び、「この設定に戻す」で内容を確認して復元します。
- コマンド未設定など未完成の設定も保存できます。該当するバックアップには警告を表示し、復元後の一括チェックで修正箇所を確認できます。
- 復元はBridge停止中に実行します。復元前の設定も必ず保存するので、そのバックアップからやり直せます。
- 書き込み失敗時は変更したファイルを元へ戻します。巻き戻しにも失敗した場合は対象ファイルと復元前バックアップIDを表示します。
- ワールド本体、サーバー接続先、RCON認証、ログイン情報、サーバーフォルダは復元対象外です。後から追加したコマンドは削除しません。
- 保存先はアプリのユーザーデータ内 `settings-backups`。サーバーごとに分かれ、別サーバーのバックアップや破損したバックアップを復元対象として読み込みません。

## 検証

`npm run verify` で型チェック、Bridgeシミュレーション、アプリ回帰テスト、本番画面ビルドを実行します。設定の往復復元、認証・接続情報の保持、復元途中の失敗からの巻き戻し、起動中の復元拒否、破損・別サーバー・パス脱出の拒否、世代数、配信前チェックの正常・停止状態を検証します。PRではWindowsとLinuxの両方で同じ検証を実行します。

終了時の確認は [Electronのwill-prevent-unload](https://www.electronjs.org/docs/latest/api/web-contents#event-will-prevent-unload) に従って実装しています。
