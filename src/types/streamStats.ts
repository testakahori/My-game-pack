export type Received = { gifts: number; coins: number; unknownCoinGifts: number; likes: number; comments: number; follows: number; shares: number; visits: number; durationMs: number; startedAt: string; updatedAt: string; giftBreakdown: { id: string; name: string; coinValue: number | null; count: number; coins: number }[] };
export type Stream = { id: string; title?: string; source?: string; roomId?: string; active?: boolean; start: string; end: string; durationMs: number; events: number; succeeded: number; failed: number; received: Received | null; earnings: { amount: number; currency: string } | null; maxViewers?: number; avgViewers?: number; viewerSamples?: number; topCommands: { name: string; count: number }[] };
export type Summary = { streams: number; trackedStreams: number; earningStreams: number; durationMs: number; gifts: number | null; coins: number | null; unknownCoinGifts: number | null; likes: number | null; follows: number | null; comments: number | null; shares: number | null; visits: number | null; earnings: number | null; coinsPerHour: number | null };
export type Stats = { streams: Stream[]; comparisons?: { days: number; current: Summary; previous: Summary }[] };
