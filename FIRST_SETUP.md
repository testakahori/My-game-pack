# MyGamePack はじめてのセットアップ（v1.0.33以降）

1. Minecraft Java版とMinecraftランチャーを用意し、一度ランチャーを開きます。
2. アプリの「初期セットアップ」で保存先を確認します。標準はドキュメント内の MyGamePack/Server です。別のドライブを使う場合は空のフォルダーを選びます。
3. 「Forgeをインストールする」で表示される画面の「Install client」を選び、OKを押します。通常はForge側の保存先を変更しません。導入済みでもボタン名は同じで、Forgeのセットアップ画面を開けます。
4. 「環境構築をする」を押し、Minecraft利用規約を確認して同意します。設定の準備に続いてワールドを生成します。初回は数分かかります。アプリを開いたまま待ってください。
5. ワールドの保存とサーバーの自動停止が終わると、完了画面が表示されます。ダッシュボードのワールド一覧に world が表示されます。
6. ダッシュボードでTikTok IDとマイクラIDを設定します。Minecraftランチャーでは 1.20.1-forge-47.3.0 を選び、サーバー起動後に接続します（標準は localhost）。TikTok LIVEの開始は別途行います。

ワールドの保存先は、選んだサーバーフォルダー内の haihu_world/world です。Forgeクライアントのインストールだけではワールドは作られません。「環境構築」で生成します。

以前の構築でルート直下に world がある場合は、中身を維持して haihu_world/world へ移動します。両方に同名ワールドがある場合は上書きせずエラーを表示します。別のワールドが設定済みの場合はその設定を保持します。

v1.0.31で構築済みでもワールドがない場合は、同じ保存先で「環境構築をする」をもう一度押してください。途中でキャンセル・失敗した場合も同じ保存先から再試行できます。パスワード・OP・ギフト設定は保持します。

生成エラーの詳細は保存先の setup-world.log に記録されます。サーバーが起動中の場合は、停止してから再試行してください。Nortonの保護停止や除外登録は不要です。
