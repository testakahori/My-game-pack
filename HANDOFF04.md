# HANDOFF04 — v1.0.21 以降（2026-07-17 〜 2026-09-21）の作業記録

HANDOFF03.md の最終記録（v1.0.21 / 自動更新404修正）以降に行われ、HANDOFF03 に
書かれていない変更をすべてまとめる。対象は PR #2〜#25、リリース v1.0.25〜v1.0.43。

- 現行バージョン: **アプリ v1.0.43 / DoumaCmd Mod 1.3.4**（Minecraft 1.20.1 / Forge 47.x）
- GitHub: `testakahori/My-game-pack`（**public リポジトリ**。秘密情報をコミットしないこと）
- 検証コマンド: `npm run verify`（型チェック → Bridge テスト → App テスト → Vite ビルド）
  - 最新時点: Bridge 14件 + App 127件 すべて成功
- CI: `.github/workflows/verify.yml`（Windows / Linux の両方で verify を実行）

---

## 1. リリース一覧

| バージョン | PR | 日付 | 内容 |
|---|---|---|---|
| —（未リリース） | #2 | 07-19 | 独立したギフトコンボを再発火可能に |
| —（未リリース） | #3 | 08-02 | ライブコメント TTS 再生の修正 |
| — | #4 | 08-02 | ライブ TTS と商用化準備の強化 |
| v1.0.24 | #5 | 09-16 | ギフト更新修正・配信前チェック・設定バックアップ/復元 |
| v1.0.25 | #6 | 09-18 | ルーレット表示修正・自動更新の安全化 |
| v1.0.26 | #7 | 09-19 | デスクトップのコピー機能・配信セッション集計の修正 |
| v1.0.27 | #8 | 09-19 | イベントテスト拡張・配信開始/終了の記録・画像保存 |
| v1.0.28 | #9 | 09-19 | 配信演出（ローカル素材・OBS出力）※#11で削除 |
| v1.0.29 | #10 | 09-19 | いいねごとの個別演出 ※#11で削除 |
| v1.0.30 | #11 | 09-20 | 配信演出を別アプリへ分離（MyGamePackから削除） |
| v1.0.32 | #12 | 09-20 | 初回 Forge インストールとワールド自動生成の修正 |
| v1.0.33 | #13, #14 | 09-20 | ギフトコマンド強化・高速ルーレット・ライブ手順変更 |
| v1.0.34 | #15 | 09-20 | 流星群・天変地異・地形攻撃の強化 |
| v1.0.35 | #16 | 09-20 | 落下ギフトの直撃・水中召喚・ウィザー攻撃（Mod 1.3.2） |
| v1.0.36 | #17 | 09-21 | OBS 用配信画像エディター・UI 刷新 |
| v1.0.37 | #18 | 09-21 | 実機のギフト画像取得を修正 |
| v1.0.38 | #19 | 09-21 | 画像編集からギフトのコマンドを設定 |
| v1.0.39 | #20 | 09-21 | ギフト設定の保存・読込（テンプレート）と画像カード削除 |
| v1.0.40 | #21 | 09-21 | MAP セーブ・ロードと配信の自動記録 |
| v1.0.41 | #22 | 09-21 | いいね不発・贈り主名の修正と BRIDGE 単体操作 |
| v1.0.42 | #23 | 09-21 | コメント読み上げ・ライブ状態・起動済みサーバーの修正 |
| （v1.0.42 追補） | #24 | 09-21 | MAP 保存を手動のみに・斜めの巨大流星群（Mod 1.3.3） |
| **v1.0.43** | #25 | 09-21 | 流星群の連続化・配信別受信統計・コマンド分類（Mod 1.3.4） |

v1.0.31 は欠番。

---

## 2. 機能別の変更内容

### 2.1 ギフト・いいね・イベント処理（Bridge）

- **コンボ再発火（#2）**: コンボ到達マーカーを恒久的な `likeMilestones` から分離し、
  ギフトバーストごとに保持。`windowMs` を越えた2回目のコンボも発火する。
- **TikTok Live Connector 2.4.3 へ移行（#4）**: `TikTokLiveConnection` API へ移行。
  初期履歴の処理を無効化、切断後の自動再接続とレート制限を考慮したバックオフを追加。
- **イベントフィールドの正規化（#22, #23）**: 2.4.3 のネストしたユーザー情報と
  `count/total` に対応し、贈り主名が unknown になる問題、100いいねごとのタラが出ない問題を修正。
  コメントは新形式 `content` を旧 `comment` へ正規化（コメント読み上げ・コメント連動を復旧）。
  過去分は一括発火せず、同じ累計・逆順・同一メッセージは二重計上しない。
  ギフト連打の終了通知による余分な召喚、未設定ギフトの二重発火も抑制。
- **設定の即時反映（#22）**: 配信中のいいね/ギフト設定は保存後、次のイベントから自動反映される
  （再起動不要。誤った再起動案内を削除）。
- 関連モジュール: `bridge/tiktok_events.cjs`、`bridge/audience_stats.cjs`、`bridge/stream_sessions.cjs`

### 2.2 TTS（コメント読み上げ）

- UI と Bridge が別の TTS 設定ファイルを読んでいた問題を修正し、Bridge の TTS 設定を唯一の正とする（#3）。
- TTS エンジンの準備完了を待ってから Bridge を起動。準備失敗時は配信起動前に明示停止（#3, #4）。
- Windows の音声再生を直列化。TTS キューは最大 50 件、設定値・文字数を検証（#4）。
- ダッシュボードで「TikTok 接続済み」と「Bridge プロセス起動中」を区別して表示（#3）。

### 2.3 ルーレット

- UI 保存済みの `commands/minecraft` にさらに `minecraft` を付けて検索し、全項目を除外していた
  「ルーレットが空になる」問題を修正（#6）。
- 配信時とアプリ内テストの演出を共通化。日本語ラベル・説明を順に表示して減速し、
  結果表示後に当選回数だけ実行。連続発動は順番に処理し、通信失敗を報告（#6）。
- 切替を約 0.05 秒に高速化し、最後だけ減速（#13）。
- ※HANDOFF03 の「ルーレット演出は2箇所に複製実装」の注意は、#6 の共通化（`bridge/roulette.cjs`）
  以降も変更時は Bridge 経由・テスト発火の両経路で確認すること。

### 2.4 ギフトコマンド（`bridge/commands/minecraft/*.txt` と Mod）

v1.0.33〜v1.0.43 で大幅に強化。詳細は `docs/release-v1.0.33.md`、`docs/release-v1.0.43.md`。

- **Mod がギフト数を受け取る**ようになり、大量召喚はサーバー tick に分散（#13）。
  スーパージャンプは 1個=32ブロック上昇、10個なら 320 ブロック（空中の追加分も加算）。
- 洪水: 48 ブロック上に 25×25 の水源、約3秒で水到着、その水中へドラウンド30体＋海の生き物153体（#13, #15, #16）。
- 弾丸タイム: 全方向から240発の矢。空からアンビル100個（5×5、外周3段・中央8段で囲む）、鶏200匹、メテオ100発、火山50発＋足元21×21を溶岩化。
- 天空トラップ: 上空85ブロックへ移動して急降下。
- ゾンビ総襲撃: 4種類（ゾンビ・ハスク・ドラウンド・村人ゾンビ）各50体（ゾグリンは除外）。
- 氷河期: 97×97の地表凍結＋5×5×3の氷塊を頭上から落下。閉じ込め中は継続ダメージ、脱出で停止、氷は残る。
- 暴風雨: 天候サイクル無効でも約3分で終了。**強制晴れ（clearweather）**を新規追加。
- **天変地異（cataclysm）** 新規: 雷・雨・亀裂・頭上マグマ・猛吹雪・TNT・ウィザー・ゾンビピグリン、
  足元に最大48ブロックの穴＋溶岩、流星群まで続く段階的演出。
- **流星群（meteorshower）** 新規 → 改良を重ね、v1.0.43 で「109ブロックの岩球を2塊ずつ連続で浅い斜め軌道」、
  5素材×各10塊＋破片2500個、発動中だけ夜→終了後に昼へ復帰、直撃ダメージ・クレーター・残骸。
  重ねがけ・サーバー終了/再起動にも対応（Mod 1.3.3〜1.3.4）。
- ウィザーはサバイバルのプレイヤーを優先して狙う。重力反転は8秒間の横移動も低下（#16）。
- Mod 側の新規クラス: `GiftEffects.java`、`JumpPhysics.java`。Mod テストは `DoumaCmdMod/test/`。

**削除したコマンド**（`electron/main.cjs` の `BRIDGE_REMOVED_BUNDLE_FILES` に登録済み）:
`seichi_donut`（砂落下を1種類に統合）、`tornado`、`hukitobasu`、`hurricane`、`husk01`（重複ハスク）、
`give_blindness`（暗闇地獄）、`petsummon`（ペット召喚）。
既存ユーザーの割り当ては **バックアップを取ってから** 残すコマンドへ移行する（`electron/command_migration.cjs`）。
「暗黒」「愛犬が来た！」は保持。

### 2.5 設定の保護・バックアップ・配信前チェック（#5）

- ギフト一覧更新にタイムアウト・同時実行抑止・取得結果検証を追加。失敗時は保存済み一覧を保持。
- 配信前の一括チェック: アカウント、設定、参照コマンド、ワールド、RCON、サーバー・Mod・LIVE 接続、読み上げ。
- 画面移動・再読込・終了時に未保存の編集を保護。保存失敗時に成功表示しない。
- 設定変更前の自動バックアップ 30 世代＋手動バックアップ、復元前の退避、復元失敗時の巻き戻し。
  認証・接続先・ワールド本体は復元対象外。操作説明は `SETTINGS_SAFETY.md`。

### 2.6 自動更新・配布（#6, #4）

- 日本語の進捗表示、ダウンロード中の再確認抑止、未保存設定がある間は適用しない、更新後もセットアップ状態を保持。
- 配布サーバーテンプレートは必要な資材だけから生成し、**ワールド・プレイヤー情報・ログ・RCON パスワードを含めない**。
- Electron バージョンを固定、Windows インストーラー生成を修復。npm audit（本体・Bridge・GiftsViewer）0件。
- clean clone で欠落する開発用ギフト JSON への依存を除去し、Bridge/Gifts ツールと Node runtime を再現可能に生成。
- 実アカウント名のハードコードを製品 UI から除去。

### 2.7 Electron のセキュリティ境界（#4, #7, #18）

- 任意ローカルページ遷移、危険な外部プロトコル、設定 IPC、パストラバーサル、無制限画像取得を制限。
- コピー機能: インストール版はブラウザのクリップボード権限が無効なため、テキスト限定・サイズ上限付きのネイティブ IPC 経由に変更（#7）。
- ギフト画像取得を Electron `net.fetch` に統一（Node の証明書ストア差で `UNABLE_TO_VERIFY_LEAF_SIGNATURE` になっていた）。
  HTTPS 限定・リダイレクト上限・画像形式・10MB 上限・タイムアウトは維持（#18, `electron/gift_image.cjs`）。

### 2.8 初回セットアップ・Forge・サーバー起動（#12, #13, #14, #23）

- 新規保存先で「forge_install.bat がない」と失敗する問題を修正。同梱 Java から Forge GUI を起動。
  Forge ボタンは常に「Forge をインストールする」で、インストーラーの「Install client」へ案内。
- 環境構築で `haihu_world/world` の生成・保存・サーバー停止まで自動実行（EULA 同意後に設定作成、進捗表示、二重起動防止）。
  既存 world は保持して移動、同名の移動先があれば上書きせず停止。未完了印で途中失敗を成功扱いしない。
- 初回の標準保存先をドキュメント内へ変更。手順は `FIRST_SETUP.md`。
- 最新版 Mod の同梱を保証するチェックを追加。
- ライブ手順の順番: **TikTok LIVE STUDIO で接続 → Forge サーバー → Minecraft → BRIDGE**。
- アプリを開き直した時、起動済みの Forge サーバーを検出（サーバーと Mod の応答確認）し、
  ワールドに触れず BRIDGE の起動へ進む。サーバー保存中の15秒強制終了を廃止し正常終了を待つ（#23）。
- ホームに BRIDGE 起動・停止・再起動を常設。単体操作ではサーバーやゲームルールを変更しない（#22）。
- TikTok 欄は BRIDGE と独立にアカウントのライブ状態を30秒ごとに確認（通信失敗は「不明」）（#23, `electron/tiktok_live_status.cjs`）。
- 関連: `electron/initial_setup.cjs`、`electron/world_setup.cjs`

### 2.9 MAP セーブ・ロード（#21, #24）

- MAP を名前付きで保存・復元（旧「保護エリア設定」を置き換え）。
- ロード時は現在の MAP を検証済みの一時退避へ逃がし、ファイル照合とサーバー正常停止を確認してから入れ替え。途中失敗・中断時は復旧。
- #24 で **自動保存を廃止し「保存する」を押した時だけ履歴に追加**。旧バージョンのジャーナル互換性は維持。
- 関連: `electron/map_saves.cjs`、`src/components/WorldSavesPanel.tsx`、`docs/MAP保存と配信統計.md`

### 2.10 配信統計（#7, #8, #21, #25）

- 配信セッションの区切りを90分に変更（約10時間離れたイベントが1配信に合算されていた）（#7）。
- 配信開始・終了を保存し、アプリ再起動をまたいで集計（#8）。#21 で TikTok の配信 ID と接続区間を
  自動記録する方式に変更し、手動の開始・終了パネルは削除。再接続は同一配信へ統合。
- v1.0.43（#25）: 配信別履歴を常時表示。コマンド発動と独立した**受信ギフト個数・コイン相当・いいね等**を長期保存
  （30日を超えて保持）、7日/30日比較、任意の確定受取額入力（未入力と0円を区別、コインから固定換算しない）、
  個別/全履歴の Markdown 出力。過去に記録していない数字は「未取得」表示。
- 関連: `electron/stream_statistics.cjs`、`electron/stream_earnings.cjs`、`electron/stats_export.cjs`、`src/types/streamStats.ts`

### 2.11 運用センター・テスト発火（#8）

- コメント・死亡・シェア・訪問・未設定ギフト・フォロー等も保存済み設定に沿ってテスト可能に。
  15種類の条件確認と Mod への送信結果を表示。テスト履歴は配信統計から除外。
- フォローの通常コマンドが無効でもマイルストーンを判定するよう修正。

### 2.12 OBS 配信画像エディター・UI 刷新（#17〜#21）

- 画像編集を独立した「配信画像」画面へ刷新: 3テンプレート、ギフト設定からの追加、内容別・カード別配色、
  見出し、並べ替え、取り消し、自動保存、縦横配置プレビュー、透過 PNG 保存（Windows 保存ダイアログ）。
  詳細 `docs/OBS配信画像.md`、`design-qa.md`。
- 各カード右上の × で1枚ずつ外せる（PNG には × を含めない）（#20）。
- 金枠カードをチャコール、ナイトステージを6列×2段に変更（#21）。
- アプリ全体: 共通ヘッダーと目的別ナビ、ホームの個別操作・ログを折りたたみ、運用センターを用途別に分割（#17）。
- 起動・停止エラーをボタン直下に表示し、ページ全体のログ追従スクロールを廃止（#23）。
- Font Awesome（`@fortawesome/fontawesome-free`）を依存に追加。
- 関連: `src/lib/giftPanel.ts`、`src/lib/giftPanelCanvas.ts`、`src/studio.css`

### 2.13 ギフト設定の保存・読込とコマンド選択（#19, #20, #25）

- #19 で画像編集からギフトのコマンド・回数を変更できるようにしたが、#21 で画像編集からは外した
  （コマンド設定はギフト設定画面側で行う）。
- ギフト割り当て全件を JSON に保存し、用途別に配布・読込できる。読込は内容確認後に置き換え、適用前に自動バックアップ。
  アカウント・パスワードは含めない。コマンド不足・設定競合・不正ファイルでは適用を止める（`electron/gift_templates.cjs`、
  `docs/ギフト設定テンプレート.md`）。画面上の「テンプレート」表記は #21 で外した。
- ギフト設定のコマンド選択にカテゴリ別表示と名前・効果の検索を追加。絞り込んでも保存済みのコマンドは消えない（#25）。

### 2.14 配信演出（#9, #10 → #11 で削除）

- v1.0.28〜v1.0.29 でローカル素材（PNG・音声・動画）を OBS に出す「配信演出」を実装したが、
  **別アプリとして扱う方針になり v1.0.30（#11）で MyGamePack から削除**。
- 演出用のユーザーデータは削除していない。旧実装は **`v1.0.29` タグ** から参照できる。
- 課金・ポイント制度・素材販売はこのアプリでは実装しない（素材販売は外部で行う想定）。

---

## 3. 既知の問題・未検証事項

- **保存ダイアログで 0xc0000409 終了**: v1.0.39 のインストール直後、最初の保存ダイアログ表示で1回だけ記録。原因未特定。
  再起動後は再発せず、v1.0.40 の MD 保存でも再現していない（`design-qa.md` に記録）。
- **実 TikTok LIVE での E2E が一部未実施**: フォロー・シェア・訪問・未設定ギフトの4イベント、受信統計の実配信投稿テスト、
  OBS 配信先までの最終出力。v1.0.41 の「100いいね→タラ・ギフト読み上げ・贈り主名」はユーザーが実配信で確認済み。
- 破壊系コマンドは通常のユーザーワールドでは未検証（専用 Forge サーバーで検証済み）。ワールドを変更する点に注意。
- 実ゲーム画面での見た目（美観）の確認は検証対象外だった項目がある。
- 統計の「コイン相当」は実際の受取額への換算ではない。接続中に取得した活動のみ記録される。

## 4. 商用配布に向けた残タスク（#4 より）

- コード署名証明書の取得（未署名インストーラー）。
- サーバー側のサブスク権限管理。
- TikTok Live Connector は公式に production-ready と保証されていないため、商用版では公式推奨の
  WebSocket API/provider への移行を検討。

## 5. 運用上の注意（HANDOFF03 からの追記・更新）

- **public リポジトリ**なので、パスワード・アカウント ID・RCON パスワード・個人的なやり取りの記録は
  コミットしない。リポジトリ外のローカル専用フォルダ（例: 親ディレクトリ側の作業メモ類）はリポジトリに入れないこと。
- コマンド txt を廃止するときは引き続き `BRIDGE_REMOVED_BUNDLE_FILES` に追記し、割り当ては
  `command_migration.cjs` でバックアップ付き移行する。
- リリース手順は HANDOFF03 末尾の通り（`release/` の .exe / .blockmap / latest.yml をそのままアップロードし、
  ダウンロード URL が 200、アセット名が latest.yml の `path` と一致することを確認）。
- ローカルの作業コピーが origin/main より古い HEAD のまま、ファイルだけ最新になっている状態が起きていた。
  作業開始前に `git fetch` と `git status --short --branch` で HEAD と origin/main の関係を確認すること。
